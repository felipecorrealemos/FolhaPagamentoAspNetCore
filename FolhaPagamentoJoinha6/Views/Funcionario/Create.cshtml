<h1>Create</h1>
<h4>Funcionario</h4>
<hr />

<div>
    <form asp-action="Salvar">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

        <partial name="_CriarFuncionarioPartial" model="new Funcionario()" />
        <h4>Endereço</h4>
        <partial name="_CriarEnderecoPartial" model="new Endereco()" />

        <div class="form-group mt-5 mb-5">
            <a class="btn btn-secondary" asp-controller="Funcionario" asp-action="Index">Back to List</a>
            <input type="submit" class="btn btn-success" />
        </div>

        <partial name="_FocusInput-btnVoltarValidade">
    </form>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}